Compilation command : gcc huffman_coding_decoding.c
Output : ./a.out

Albhabets and frequency are pre feteched in code. If we want to change the input please go in main() function and change the frequency.
